# react-typescript-RPC

npm install

npm start

To run few selected tests run:

npm test

With more time I would have:

- Create a more comprehensive test suite. Have added a few tests just to illustrate.
